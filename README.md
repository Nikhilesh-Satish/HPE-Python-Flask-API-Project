To run the backend folder, clone the repository first. 
Then, use the command pip install -r requirements.txt to install dependencies and libraries
In the project root type python app.py to start the backend server.
The API routes are present in the code and can be used to get, post, put, delete, patch, get users by id and get users by partial city name.
Additionally the ids are allocated and auto incremented. 
